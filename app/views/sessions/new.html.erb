<% provide(:title, "Log In") %>

<h1>Log In</h1>

<%= form_for :session do |f| %>
	<%= f.label 			:name %>
	<%= f.text_field 	:name %>

	<%= f.submit("Log In") %>

<% end %>

<h3>
	Don't have an account?
	<%= link_to "register for free", signup_path %>
</h3>